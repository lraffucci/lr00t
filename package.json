{
  "name": "app",
  "description": "This is for my own ammusement. You have been warned.",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "author": "Luis Raffucci",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lraffucci/lr00t.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node app.js",
    "test": "mocha --exit test/*.test.js",
    "prodBuild": "dir && cd frontend && npm run build && cd .. && node app.js",
    "dev": "npm run devBuildFrontend && dir && cd frontend && npm run start && cd .. && nodemon --inspect app.js",
    "devBuildFrontend": "dir && cd frontend && npm run start",
    "debugServer": "dir && cd frontend && npm run build && cd .. && nodemon --inspect app.js"
  },
  "dependencies": {
    "express": "^4.16.3",
    "nodemon": "^2.0.21",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "mocha": "^7.0.0",
    "supertest": "^4.0.2"
  },
  "bugs": {
    "url": "https://github.com/lraffucci/lr00t/issues"
  },
  "homepage": "https://github.com/lraffucci/lr00t#readme",
  "main": "app.js",
  "directories": {
    "test": "test"
  }
}
